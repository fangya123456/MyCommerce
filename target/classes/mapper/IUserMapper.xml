<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fy.commerce.mapper.IUserMapper">

    <resultMap id="UserResultMap" type="com.fy.commerce.model.User">
        <id column="uid" property="uid" jdbcType="INTEGER" />
        <result column="username" property="username" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="sex" property="sex" jdbcType="VARCHAR" />
        <result column="email" property="email" jdbcType="VARCHAR" />
        <result column="phone" property="phone" jdbcType="VARCHAR" />
        <result column="birthday" property="birthday" jdbcType="VARCHAR" />
        <result column="addr" property="addr" jdbcType="VARCHAR" />
        <result column="code" property="code" jdbcType="VARCHAR" />
    </resultMap>

    <select id="selectUserInfoById" parameterType="int" resultMap="UserResultMap">
        select * from user where uid=#{uid};
    </select>

    <select id="selectUserInfoByExample" parameterType="com.fy.commerce.model.User" resultMap="UserResultMap">
        select * from user where uid=#{uid} or username=#{username} or email=#{email};
    </select>
    <insert id="insertUserInfoByExample" parameterType="com.fy.commerce.model.User" useGeneratedKeys="true"
            keyProperty="uid">
        insert into user(username, password, name, sex, email, phone, birthday, addr,state, code) values(#{username}, #{password}, #{name}, #{sex}, #{email}, #{phone}, #{birthday}, #{addr},#{state}, #{code});
    </insert>
</mapper>